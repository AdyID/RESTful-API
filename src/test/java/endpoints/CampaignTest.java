package endpoints;

import org.junit.Assert;
import org.junit.Test;

import java.time.LocalDateTime;

import static org.junit.Assert.*;

public class CampaignTest {
/*
    private final Campaign campaign = new Campaign(14,"FireCampaign", 1212, 4);

    @Test
    public void getId() throws Exception {
        assertEquals(campaign.getId(), 14);
    }

    @Test
    public void getName() throws Exception {
        assertEquals(campaign.getName(), "FireCampaign");
    }

    @Test
    public void getCreated() throws Exception {
    }

    @Test
    public void getClicks() throws Exception {
        assertEquals(campaign.getClicks(),1212);
    }

    @Test
    public void getImpressions() throws Exception {
        assertEquals(campaign.getImpressions(),4);
    }

    @Test
    public void getBanners() throws Exception {
    }

    @Test
    public void testDateCreated(){
        LocalDateTime time = LocalDateTime.now();
        Campaign campaign = new Campaign(LocalDateTime.now());
        campaign.getCreated();
        Assert.assertTrue(time.isBefore(campaign.getCreated()) || time.isEqual(campaign.getCreated()));
    }
*/
}